import React from 'react';

/* export default function Hello({children}){ //Desestruturação
    return(
        <h1>
            <div>
                {children}
            </div>
        </h1>
    )
} */

/* const Hello =({text})=>(<h1>{text}</h1>)

export default Hello; */

const Hello = ({text}) => <h1>{text}</h1>

export default Hello;